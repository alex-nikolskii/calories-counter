import { Container, Typography } from '@mui/material';

export function SettingsPage() {
    return (
        <Container>
            <Typography variant="h4" gutterBottom>
                Настройки
            </Typography>
            {/* Смена цели, языка, сброс данных и т.п. */}
        </Container>
    );
}
