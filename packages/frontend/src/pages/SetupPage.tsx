import { Container, Typography } from '@mui/material';

export function SetupPage() {
    return (
        <Container>
            <Typography variant="h4" gutterBottom>
                Первичная настройка
            </Typography>
            {/* Форма: рост, вес, возраст, цель */}
        </Container>
    );
}
